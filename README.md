# 🚀 Nav Hub - 现代化导航站

一个简洁、高效、可自定义的个人导航站，基于 Next.js 16 + React 19 构建。

![Next.js](https://img.shields.io/badge/Next.js-16-black?style=flat-square&logo=next.js)
![React](https://img.shields.io/badge/React-19-blue?style=flat-square&logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?style=flat-square&logo=typescript)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-4-38B2AC?style=flat-square&logo=tailwind-css)

## ✨ 特性

- 🎨 **现代化 UI** - 简洁美观的界面设计，支持明暗主题切换
- 📱 **响应式布局** - 瀑布流自适应布局，完美适配各种屏幕尺寸
- 🔍 **多引擎搜索** - 支持 Google、Bing、百度搜索引擎切换
- 🖱️ **拖拽排序** - 分类和链接均支持自由拖拽排序
- ⚡ **本地存储** - 数据持久化到 localStorage，无需后端
- 🎯 **右键菜单** - 便捷的右键操作，快速编辑和删除
- 🖼️ **智能图标** - 自动获取网站 favicon，支持自定义文字图标
- ⌨️ **快捷键** - 按 `/` 快速聚焦搜索框

## 🛠️ 技术栈

- **框架**: Next.js 16 (App Router)
- **UI**: React 19 + Tailwind CSS v4
- **组件库**: shadcn/ui + Radix UI
- **状态管理**: Zustand
- **拖拽**: @dnd-kit
- **动画**: Framer Motion
- **主题**: next-themes
- **语言**: TypeScript 5

## 🚀 快速开始

### 安装依赖

```bash
npm install
# 或
pnpm install
# 或
yarn install
```

### 启动开发服务器

```bash
npm run dev
# 或
pnpm dev
# 或
yarn dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看效果。

### 构建生产版本

```bash
npm run build
npm run start
```

## 📁 项目结构

```
src/
├── app/                    # Next.js App Router
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   └── globals.css         # 全局样式
├── components/             # 通用组件
│   ├── ui/                 # shadcn/ui 组件
│   ├── theme-provider.tsx  # 主题提供者
│   └── theme-toggle.tsx    # 主题切换按钮
├── features/               # 功能模块
│   └── navigation/         # 导航功能
│       ├── components/     # 导航相关组件
│       └── index.ts        # 导出入口
├── stores/                 # Zustand 状态管理
│   └── navigation-store.ts # 导航数据 store
├── data/                   # 默认数据
│   └── navigation-data.ts  # 导航链接数据
├── lib/                    # 工具函数
│   ├── utils.ts            # 通用工具
│   └── favicon-cache.ts    # favicon 缓存
└── types/                  # TypeScript 类型
    └── navigation.ts       # 导航类型定义
```

## 🎯 功能说明

### 分类管理
- 点击「添加分类」创建新分类
- 拖拽分类标题可调整分类顺序
- 右键分类标题可添加链接或删除分类

### 链接管理
- 点击「添加链接」添加新链接
- 左键点击链接直接跳转
- 右键点击链接可自定义或删除
- 拖拽链接左侧手柄可调整顺序

### 自定义图标
- 默认自动获取网站 favicon
- 可切换为文字图标模式
- 支持自定义图标背景颜色

### 搜索功能
- 点击搜索框左侧图标切换搜索引擎
- 按 `/` 键快速聚焦搜索框
- 按 `Escape` 键清空搜索内容

## 🔧 自定义配置

### 修改默认数据

编辑 `src/data/navigation-data.ts` 文件来自定义默认导航数据。

### 重置数据

清除浏览器 localStorage 中的 `navigation-storage` 键即可重置为默认数据。

## 📄 License

MIT License - 随意使用和修改

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

如果觉得这个项目有帮助，欢迎给个 ⭐ Star！
